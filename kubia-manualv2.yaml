apiVersion: v1
kind: Pod
metadata:
  name: kubia-manual-v2
  labels:
    env: prod
    creation_method: manual
spec:
  containers:
  - name: kubia
    image: luksa/kubia
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 8080
