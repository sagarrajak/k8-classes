apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: default
  labels:
    app: mysql-pvc
spec:
  storageClassName: ""  # this is to use the 
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi