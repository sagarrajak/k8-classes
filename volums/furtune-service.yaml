apiVersion: v1
kind: Service
metadata:
  name: fortune-service
  namespace: default
spec:
  selector:
    app: furtune
  type: NodePort
  ports:
  - name: http
    protocol: TCP
    port: 5000
    targetPort: 80
    nodePort: 30080