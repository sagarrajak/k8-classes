apiVersion: v1
kind: Service
metadata:
  name: kubia-nodeport
  namespace: replicaset-example
spec:
  selector:
    app: kubia
  type: NodePort
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080
    nodePort: 30001